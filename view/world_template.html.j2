<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <link rel="stylesheet" href="http://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.3.0/css/font-awesome.min.css">
        <style type="text/css">
            %% include "w3.css"

            %% include "w3-theme-black.css"

            %% include "style.css"

        </style>
        <title>Atlassian permissions</title>
    </head>
    <body style="text-align: center">
        <h1 class="w3-xxxlarge w3-animate-bottom">Atlassian permissions</h1>

        %% for service in world.services.values()
        <div class="w3-responsive w3-card-4 service-container" style="display: inline-block; margin: 20px; vertical-align: top;"> <!-- TODO -->
        <h2 class="w3-xlarge">{{ service.name }}</h2>
            <table class="w3-table w3-striped w3-white" style="width: auto"> <!-- TODO -->
                <thead>
                    <tr class="w3-theme">
                        <th>Project</th>
                        <th>Permission</th>
                        <th>Users</th>
                        <th>Groups</th>
                    </tr>
                </thead>
                <tbody>
                    %% for project in service.projects.values()
                    %% for permission in project.permissions.values()
                    <tr>
                        <td>{{project.key}}</td>
                        <td>{{permission.name}}</td>
                        <td>
                            <ul class="w3-ul w3-margin-bottom">
                                %% for user in permission.users
                                <li>
                                    {{ user }}
                                </li>
                                %% endfor
                            </ul>
                        </td>
                        <td>
                            <ul class="w3-ul w3-margin-bottom">
                                %% for group in permission.groups
                                <li>
                                    {{ group }}
                                </li>
                                %% endfor
                            </ul>
                        </td>
                    </tr>
                    ## end of permission loop
                    %% endfor

                    ## end of project loop
                    %% endfor
                </tbody>
            </table>
            </div>
        %% endfor
    </body>
</html>
